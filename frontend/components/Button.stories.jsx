import React from 'react';
import Button from './Button.jsx';

export default {
  title: 'Components/Button',
  component: Button,
};

export const Primary = {
  args: {
    children: 'Primary',
    variant: 'primary',
  },
};

export const Accent = {
  args: {
    children: 'Accent',
    variant: 'accent',
  },
};
